import { StyleSheet } from 'react-native'

import { colors } from '../../../constants'

export default StyleSheet.create({
	container: {
		alignItems: 'center'
	},
	icon: { width: 45, height: 45 },
	title: { fontSize: 12, fontWeight: '600', color: colors.black, marginTop: 10 }
})
